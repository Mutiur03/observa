[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "observa-python"
version = "0.1.2"
description = "Minimal Observa Python SDK"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.2"]

[tool.hatch.build.targets.wheel]
packages = ["observa"]
